[package]
name = "{{project_name}}"
version = "0.1.0"
edition = "2024"
license = "MIT"
authors = ["{{author}}"]
description = "{{project_name}}"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"